/**
 * The server actions
 */
export /*actions*/ /*bundle*/
class City {
    temperature(city: string): string {
        return `27 degrees at ${city}`;
    }

    time(city: string): string {
        void city;
        return Date().toString();
    }
}
